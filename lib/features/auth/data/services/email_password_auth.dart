/// Legacy email/password auth via Supabase — no longer used with NestJS backend.
/// Use [AuthService.signInWithEmail] and [AuthService.signUpWithEmail] instead.
class EmailPasswordAuth {
  EmailPasswordAuth(dynamic _);
}
